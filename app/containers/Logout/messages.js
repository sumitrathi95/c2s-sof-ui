/*
 * Logout Messages
 *
 * This contains all the text for the Logout component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  logoutMenuItem: {
    id: 'c2s.containers.Logout.logoutMenuItem',
    defaultMessage: 'Logout',
  },
});
