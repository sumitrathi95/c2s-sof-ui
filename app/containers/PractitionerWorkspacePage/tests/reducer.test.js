// import practitionerWorkspacePageReducer from '../reducer';

describe('practitionerWorkspacePageReducer', () => {
  it('returns the initial state', () => {
    // FIXME: add unit tests
    // expect(practitionerWorkspacePageReducer(undefined, {})).toEqual(fromJS({}));
  });
});
