
// import { fromJS } from 'immutable';
// import searchAppointmentParticipantReducer from '../reducer';

describe('searchAppointmentParticipantReducer', () => {
  it('returns the initial state', () => {
    // FIXME: add unit tests
    // expect(searchAppointmentParticipantReducer(undefined, {})).toEqual(fromJS({}));
  });
});
