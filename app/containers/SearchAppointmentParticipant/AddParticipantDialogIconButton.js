import IconButton from 'material-ui/IconButton';
import styled from 'styled-components';

const AddParticipantDialogIconButton = styled(IconButton).attrs({
  style: { top: '26px', height: '30px' },
})('');

AddParticipantDialogIconButton.propTypes = {};

export default AddParticipantDialogIconButton;
