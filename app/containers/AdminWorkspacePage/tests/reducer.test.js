// import adminWorkspacePageReducer from '../reducer';

describe('adminWorkspacePageReducer', () => {
  it('returns the initial state', () => {
    // FIXME: add unit tests
    // expect(adminWorkspacePageReducer(undefined, {})).toEqual(fromJS({}));
  });
});
