// import patientWorkspacePageReducer from '../reducer';

describe('patientWorkspacePageReducer', () => {
  it('returns the initial state', () => {
    // FIXME: add unit tests
    // expect(patientWorkspacePageReducer(undefined, {})).toEqual(fromJS({}));
  });
});
