
/* import { fromJS } from 'immutable';
import practitionerConsentsReducer from '../reducer';*/

describe('practitionerConsentsReducer', () => {
  it('returns the initial state', () => {
    /* expect(practitionerConsentsReducer(undefined, {})).toEqual(fromJS({}));*/
  });
});
